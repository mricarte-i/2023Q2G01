package org.example.distance_methods;

import org.example.points.Point2D;

public class EuclideanDistanceMethod2D extends EuclideanDistanceMethod<Point2D> {
    @Override
    public Double calculateDistance(Point2D a, Point2D b) {
        return super.calculateDistance(a, b);
    }
}
